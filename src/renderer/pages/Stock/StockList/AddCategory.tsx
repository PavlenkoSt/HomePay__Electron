import styles from './styles.module.scss'

const AddCategory = () => {
  return (
    <div className={styles.category}>
      <h2 className={styles.categoryName}>Добавить новую категорию</h2>
    </div>
  )
}

export default AddCategory
